{
  "name": "monster-merge-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests e2e scripts --max-warnings 0",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "test:e2e": "playwright test",
    "profile:physics": "node --experimental-strip-types scripts/profile-physics.mjs",
    "profile:bundle": "node scripts/profile-bundle.mjs",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "verify": "npm run typecheck && npm run lint && npm run test && npm run profile:physics && npm run build && npm run profile:bundle && npm run audit:prod",
    "deploy:cloudflare": "wrangler deploy",
    "test:e2e:chromium": "playwright test e2e/app.spec.ts --project=mobile-chromium --project=desktop-chromium --grep-invert \"idle render loop stays inside a safe frame budget\"",
    "test:e2e:webkit": "playwright test e2e/app.spec.ts --project=mobile-webkit --project=desktop-webkit --grep-invert \"idle render loop stays inside a safe frame budget\"",
    "test:e2e:offline": "playwright test e2e/offline.spec.ts --project=desktop-chromium",
    "test:e2e:performance": "playwright test e2e/app.spec.ts --project=desktop-chromium --grep \"stays inside a safe frame budget\"",
    "verify:audit": "npm run typecheck && npm run lint && npm run test && node --check public/sw.js && npm run audit:prod",
    "verify:performance": "npm run profile:physics && npm run build && npm run profile:bundle"
  },
  "dependencies": {
    "lucide-react": "^0.469.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "eslint": "10.10.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "globals": "17.12.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "8.70.0",
    "vite": "^6.0.0",
    "wrangler": "^4.135.0",
    "@playwright/test": "^1.55.0",
    "@axe-core/playwright": "^4.10.2"
  }
}
